{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "JWT_SECRET",
      "description": "Secret used to sign JWT auth tokens for the Express API. Any random string works for local dev.",
      "requiredAtBoot": true,
      "generatedForDevelopment": true
    }
  ]
}
